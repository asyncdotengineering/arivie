id: total-revenue
category: normal
question: "What is our total revenue from completed orders?"
golden_sql: |
  SELECT COALESCE(SUM(total_amount), 0) AS revenue
  FROM orders
  WHERE status = 'completed'
validation:
  - result_has_rows: true
  - column_exists: revenue
  - value_positive: revenue
