{
  "name": "support-agent",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Aurora Support — a non-analytics customer-support agent built entirely on the Arivie context layer (always-inject policy + auto-retrieved FAQ). Zero infra.",
  "scripts": {
    "chat": "arivie chat --config arivie.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.64",
    "@arivie/core": "workspace:*"
  },
  "devDependencies": {
    "@arivie/cli": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.3",
    "typescript": "^6.0.0"
  }
}
