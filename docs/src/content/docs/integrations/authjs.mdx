---
title: Auth.js (NextAuth v4)
description: Integrate Arivie with NextAuth v4 session email as ownerId.
source: "RFC-002 §3.2 + examples/with-authjs"
---

import { Code } from '@astrojs/starlight/components';
import routeTs from '../../../../../examples/with-authjs/app/api/arivie/route.ts?raw';
import resolveOwner from '../../../../../examples/with-authjs/lib/resolve-owner.ts?raw';

Use **Auth.js** (NextAuth **v4** stable) when your team already ships `next-auth` with the App Router and you want a credentials or OAuth provider with minimal migration risk. Auth.js v5 remains beta as of the example pin date.

## Setup

1. Copy `examples/with-authjs/.env.example`:

   - `DATABASE_URL`
   - `NEXTAUTH_SECRET`, `NEXTAUTH_URL`
   - Model keys (optional)

2. Install:

   ```bash
   pnpm add next-auth@^4.24.14
   ```

3. Configure `lib/auth-options.ts` (credentials stub for CI) per the example.

## Route handler

<Code code={routeTs} lang="typescript" title="app/api/arivie/route.ts" />

## ownerId derivation

<Code code={resolveOwner} lang="typescript" title="lib/resolve-owner.ts" />

`getServerSession(authOptions)` → `session.user.email` becomes `ownerId` when set.

## Runnable example

```bash
pnpm --filter with-authjs dev
```

Canonical tree: `arivie/examples/with-authjs/`.

_Tested with `next-auth ^4.24.14`._
